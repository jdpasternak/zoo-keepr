const {
  filterByQuery,
  findById,
  createNewAnimal,
  validateAnimal,
} = require("../lib/animals");

descirbe("filterByQuery", () => {
    describe('should return all animals with carnivore diet')
})
